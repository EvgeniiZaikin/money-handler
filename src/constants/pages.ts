export enum ROUTES {
  ROOT = '/',
  EXPENSES = '/control',
  STATISTIC = '/statistic',
  DYNAMIC = '/dynamic',
}

export enum PAGES {
  EXPENSES = 'Расходы',
  STATISTIC = 'Статистика',
  DYNAMIC = 'Динамика',
}
